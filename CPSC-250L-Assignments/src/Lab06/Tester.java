package Lab06;

import static org.junit.Assert.assertEquals;
import static org.junit.Assert.assertFalse;
import static org.junit.Assert.assertTrue;
import static org.junit.Assert.fail;

import java.io.File;
import java.io.IOException;
import java.io.PrintWriter;
import java.util.Scanner;


public class Tester {

	public static void main(String[] args) {
		
	    
		
		/*
		try {
			// create file
			File        input  = new File("C:\\Users\\Walker\\Desktop\\Programming\\Labs\\Lab06 folder\\input.txt");
			File        output = new File( "C:\\Users\\Walker\\Desktop\\Programming\\Labs\\Lab06 folder\\output.txt");

			PrintWriter write  = new PrintWriter( input );
			write.println( "The Adventures of Elmo in Grouchland,Sesame Street,2:17,3:04,3:16,2:31,4:28,12:42,2:13,2:24,3:32" );
			write.println( "Elmopalooza!,Sesame Street,2:37,3:59,1:40,2:35,2:31,2:24,3:34,2:45,2:34,3:29,3:15" );
			write.close();

			// invoke program
			Discography.writeReport( input, output );
			
			
		} 
		catch (IOException e) {
			fail( "No exception should be thrown" );
		}
		*/
		
		
		
		/*
		String num = "2:37";
		double sum = 0.0;
		double integer = 0;
		double dec = 0.0;
		String next = "";
		
		
		Scanner scan = new Scanner(num);
		scan.useDelimiter(":");
		
		integer += Double.parseDouble(scan.next());
	
		next += scan.next();
		System.out.println(next.length());
		
		dec += Double.parseDouble(next);
		
		sum += integer;
		
		sum += (dec / (Math.pow((double) 10, (double) next.length()))) ;
		System.out.println(sum);
		
		*/
	}

}
